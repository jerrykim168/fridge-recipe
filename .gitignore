# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store

# typescript
*.tsbuildinfo
next-env.d.ts

# local env files (contain secrets — never commit)
.env
.env*.local
openrouter.env
openrouter_api_key.env

# sqlite database (Step 3 — auth + saved recipes; see lib/db/client.ts)
/data/
*.sqlite
*.sqlite3
*.db

# contains plaintext credentials (Supabase password) — never commit
setting_etc.txt
